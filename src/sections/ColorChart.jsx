import { colorChart } from "../assets/images";

const ColorChart = () => {
  return (
    <section>
      <img className="w-full" src={colorChart} alt="color chart image" />
    </section>
  );
};

export default ColorChart;
